name: vpn_over_proxy 

services:
  # VPN
  vpn:
    container_name: VPN
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./profile.ovpn:/opt/openvpn/profile.ovpn:ro
    devices:
      - "/dev/net/tun:/dev/net/tun"
    cap_add:
      - NET_ADMIN
    network_mode: host
